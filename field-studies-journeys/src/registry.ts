import {NATIVE_BINDINGS} from './nativeParameters';
/** Every live control is a projection of its native owner. Historical preview-only values remain explicitly read-only. */
export interface Parameter {key:string;label:string;group:string;min:number;max:number;hardMin:number;hardMax:number;step:number;preview:boolean;unit?:string;note?:string;bind:string;scale?:'linear'|'log';inactive?:string}
const inactive:Record<string,string>={native_autoMorphDuration:'Compatibility value retained. The production entity path uses the two phase rates and each sequence’s timing, not this legacy auto-morph timer.'};
const legacy:Record<string,string>={damping:'Preview damping has no defined native unit. Use Viscosity Damp under Physics.',flow:'The preview fluid-coupling macro has no reversible native law. Use curl, return spring and viscosity.',resonanceDamping:'This preview scalar is not Q. Use the native resonator Q factor.',thickness:'Preview stroke dilation is not a native target parameter. Source geometry is unchanged.',warp:'Preview shape warp is not a native force law. Use the Hopf trajectory and the field’s flow.',jitter:'Preview lattice jitter is not the native target sampler. The value is retained, not applied.'};
export const PARAMETERS:Parameter[]=[...NATIVE_BINDINGS.map(b=>({...b,preview:true,inactive:inactive[b.key],note:b.path==='cymatics.sweepSpeed'?'Legacy glide fallback per station, consumed only while explicit Sweep Glide is absent. An imported explicit glide remains authoritative.':b.path==='cymatics.modeCount'?'Requested modal budget. The native model also retains its seven marked stations; the GPU addresses every active slot.':b.note??(b.key==='timeScale'?'Values at or below zero hold the simulation. Negative values do not reverse accumulated physics.':undefined)})),...Object.entries(legacy).map(([key,note])=>({key,label:key,group:'retained',min:0,max:1,hardMin:-100000,hardMax:100000,step:.01,preview:false,bind:'field.params.'+key,inactive:note,note}))];
export const parameter=(key:string)=>PARAMETERS.find(p=>p.key===key);
