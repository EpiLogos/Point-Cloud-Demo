# Native parity completion

## Locked sources
Approved Expressions shell: `9bf229dfeddfcacc3839f68662ec65e82afbd48b`.
Current entity-native reference: `63e650d26a04447b973f73dded6b5046a0829e84` (master).
Historical independent main: `040627d0ea40032fe7c7b8c98be9a246be9db7f6`.
The main and master histories have no common ancestor. Main's obsolete application
switches are not assumed to supersede master's explicit Field/Entity/Composition
model. Retain both immutable checkouts; test native source, not completion prose.

## Execution
1. Lossless native configuration projection and independent fixture round trips.
2. Native sampling/scale fidelity and declared defect corrections.
3. Contextual source, palette, sequence and command coverage without shell redesign.
4. Independent native differential tests plus full end-to-end and GPU regressions.
5. Exact-source publication, parity/divergence evidence, portable build; no merge.

A registry entry, retained payload or legacy-only route is not a passing feature.
The machine-readable ledger distinguishes implementation from verification and
records unsupported preview macros and historical supersessions explicitly.
