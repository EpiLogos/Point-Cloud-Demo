/** Fit all rows and columns, including large drawings and user-selected font sizes. */
export function asciiLayout(text:string,width:number,height:number,requested?:number){const lines=text.replace(/\r\n?/g,'\n').replace(/\t/g,'    ').split('\n'),columns=Math.max(1,...lines.map(l=>Array.from(l).length));const fit=Math.min(width*.82/(columns*.6),height*.82/(Math.max(1,lines.length)*1.15));const fontSize=Math.max(.1,Math.min(requested&&requested>0?requested:72,fit));return {lines,fontSize,charWidth:fontSize*.6,lineHeight:fontSize*1.15};}
